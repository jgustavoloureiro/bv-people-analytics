# requirements-pyspark.txt — Dependências PySpark (produção / cluster)
# Instalar: pip install -r requirements-pyspark.txt
#
# NOTA: Em clusters Databricks / Glue, PySpark já está disponível no ambiente.
# Use este arquivo apenas para execução LOCAL ou em containers Docker.

# ── PySpark ────────────────────────────────────────────────────────────────────
pyspark>=3.5.0
delta-spark>=3.0.0        # Delta Lake para Spark local

# ── AWS SDK ────────────────────────────────────────────────────────────────────
boto3>=1.34.0             # AWS: S3, Glue, Redshift, Secrets Manager
botocore>=1.34.0
s3fs>=2024.2.0            # S3 como filesystem para pandas/pyarrow

# ── Azure SDK ──────────────────────────────────────────────────────────────────
azure-storage-file-datalake>=12.15.0   # ADLS Gen2
azure-identity>=1.15.0                 # Autenticação Azure
adlfs>=2024.2.0                        # ADLS como filesystem

# ── Airflow (orquestração local) ───────────────────────────────────────────────
# apache-airflow>=2.8.0              # Descomente para setup local do Airflow
# apache-airflow-providers-amazon>=8.0.0
# apache-airflow-providers-databricks>=6.0.0

# ── MLOps / Model Serving ──────────────────────────────────────────────────────
mlflow>=2.10.0
